"use strict" // Treat all JS code as the newer version of the JS

// There's a different syntax to use alert( 6 - 58) in the node normally can use  in html binding or console in inspect 
// Arithmetic operations
// Work on readability of the code
// Documentation of MDN -> written by mozila and ecma is the real .... can read later 
// null => standalone value ... very much required as say the temperature say asked then 0 isn't looked 
// undefined => variable still not alone
// symbol => unique ... much used in react
// object

console.log(typeof undefined) // undefined

console.log(typeof null) // object